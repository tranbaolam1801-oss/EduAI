from app.agents.base_agent import BaseAgent

agent = BaseAgent(
    key="report",
    name="Report Agent",
    description="Prepare structured report generation for analytics and progress summaries.",
)
