from app.agents.base_agent import BaseAgent

agent = BaseAgent(
    key="job_opportunity",
    name="Job Opportunity Agent",
    description="Support future job matching and opportunity recommendation workflows.",
)
